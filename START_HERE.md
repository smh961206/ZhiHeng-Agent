# START HERE

给 Codex：

1. 读取 `AGENTS.md`。
2. 读取 `docs/releases/CURRENT`。
3. 当前为 H0 时，不允许修改任何投资研究运行行为。
4. 当前为某个正式 Release 时，只允许实施该 Release 的范围。
5. 未来版本文档是导航，不是提前实现授权。
6. 所有 CURRENT / PARTIAL / FUTURE 状态必须严格区分。
7. 如果本文档与实际代码冲突，先记录冲突；不得为了“符合文档”而破坏现有安全行为。
