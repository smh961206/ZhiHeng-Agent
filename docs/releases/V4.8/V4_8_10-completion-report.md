# V4.8.10 Completion Report

Status: CURRENT internal/offline implementation accepted. No production policy or live quality acceptance.

1. Release: V4.8.10 safe escalation, following accepted .8/.9. User selected MAIN GLM-5.3-Flash and PRO DeepSeek-V4-Pro, explicitly deferred paid comparison and required legacy default.
2. Modified: server/model-catalog.mjs, model-connection.mjs, model-adapter.mjs, model-state.mjs, model-gateway.mjs, model-telemetry.mjs, agent.mjs, index.mjs; tests/model-state.integration.mjs; Harness contract/invariants/ADR/maps/specifications/schema/migration/rollback/index/README and MANIFEST.
3. New: server/model-escalation.mjs, tests/model-escalation.test.mjs, this report. Ignored stage logs and rollback baseline.
4. Removed: none.
5. Architecture: same Catalog/Gateway/adapter. ModelProfile v2 explicit main/pro bindings, separate server connection refs. No inference from legacy names. Existing research-context packs complete evidence/tool records and marks omissions; new transition owner rebuilds context without raw assistant reasoning/tool history. No parallel researcher, validator, transport or financial engine.
6. Schema: ModelProfile v2 is config-only. Private modelState v2 extends .9 with active profile/effort, bounded failure counters and up to three contiguous escalation receipts. Old v1/absence remain unchanged; Mongo schema stays 2. This corrects the generic specification's no-schema wording: new internal policy jobs need a versioned additive payload, not a rewrite of v1.
7. Migration: none/backfill none. Policy state is explicit internal creation only in .10; normal creator still writes legacy v1. Compatible readers validate both versions; malformed v2 never becomes v1/absence.
8. Environment: no local .env/dependency/lockfile changes. MAIN uses LLM_MAIN_API_KEY and optional LLM_MAIN_BASE_URL (Z.ai official base by default); PRO uses LLM_PRO_API_KEY/BASE_URL or existing analysis configuration. Explicit models use LLM_MAIN_MODEL/LLM_PRO_MODEL with the user-selected defaults; different model bindings fail closed until separately supported.
9. Compatibility: v1 legacy wires/goldens preserved. v2 requires explicit effort, maps low/high/max to thinking enabled + reasoning_effort. Off/medium/unset rejected rather than silently taking model defaults. jsonSchema support remains unknown and uses existing explicit auto format negotiation; Evidence/review validation is unchanged. Initial synthetic integration caught an unsupported-capability declaration; corrected unknown metadata, not financial/review assertions.
10. Recovery: transitions require no pending tools, a saved checkpoint, context rebuild and an acknowledged strict save before dispatch. Periodic best-effort checkpoint flush is insufficient; index supplies a strict save after draining pending writes. Uncertain save stops all subsequent calls, retaining a consistent new boundary in memory/any committed payload. No finished tools replayed; cutoff/sources unchanged.
11. Flags: internal createPolicyJobModelState only; production activation remains .11 and disabled. MAIN low→MAIN high→PRO high→PRO max; Mode A never escalates. Only repeated tool argument JSON decoding failures or review_json failures qualify, not data/health/refusal/financial validation/truncation. Existing turn and review budgets never increase.
12. Tests: baseline Catalog/Gateway/state/review-format subset 53; pnpm test 671; node --test tests/model-state.integration.mjs tests/model-telemetry.integration.mjs tests/research-resume.integration.mjs tests/knowledge-api.integration.mjs 7; Harness 7 after packaging. New escalation file includes 6 tests, actual Agent tool-error and review-error scenarios as well as the complete ladder.
13. Results: final tests all pass, zero fail/skip/cancel. Mongo used isolated 27029 random test databases. Both legacy and policy process-restart/key-rotation tests pass, changed configuration blocks. Logs artifacts/v4-8-10-{baseline,unit-final,integration,harness}.log and escalation-final.log (first five before additional effort test, covered by full suite).
14. Benchmark: offline safety and unchanged goldens only. No paid calls or empirical comparison; cannot claim candidate quality or authorize rollout. .11 will retain a closed live-quality gate.
15. Privacy: ModelCall records actual policy/profile without raw reasoning. Rebuilt context includes original trusted instructions/user task, actual source/tool/evidence windows, disclosed errors/omissions and explicitly unverified draft. Hidden assistant reasoning is excluded. Provider endpoints/keys stay server-side.
16. Rollback: legacy remains current default; no production migration to undo. Retain v1/v2 pin-compatible reader for internal saved policy jobs; restore pinned model configuration to resume, never silently rewrite them as legacy. Stage rollback copies preserve accepted .9.
17. Limits: synthetic tests do not establish GLM/DeepSeek quality or hosted account capabilities. JSON argument errors are deliberately narrower than all tool/business failures. Context windows may omit complete records with explicit omission receipts; full checkpoint remains preserved. No automatic cross-provider replay after transport failure, no upgrade for missing data.
18. Deferred: real paid quality gate, production rollout/partial deployment .11, future Judge/Champion/V4.9. CURRENT remains V4.8.

Protocol references checked 2026-09-10: [Z.ai GLM-5.3-Flash](https://docs.z.ai/guides/llm/glm-5.3-flash), [Z.ai thinking](https://docs.z.ai/guides/capabilities/thinking), [DeepSeek thinking](https://api-docs.deepseek.com/guides/thinking_mode/). These establish documented effort controls, not measured research quality. The chosen low/high/max wire mapping lives only in the adapter.
