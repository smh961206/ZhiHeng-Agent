# V4.9 第二轮整体回归与优化

日期：2026-09-11。范围：当前工作区 V4.9；在上一轮优化完成后重新检查发布清单、单元、数据库/API、恢复、生产构建页面、部署、视觉离线链路及已批准的真实质量证据。沿用当前 Knowledge V4.7，未改变研究规则。

本轮新工件统一保存在 `artifacts/v49-regression-review2-20260911/`，上轮报告和原始失败记录保留。测试使用本地合成 API 数据、隔离测试 MongoDB 和临时 Docker 项目；未向真实模型发起新请求。

## 发现、修正及复核

| 项目 | 首轮发现 | 本轮处理 |
| --- | --- | --- |
| 发布清单漂移 | 单元 791/794；3 项 Vision 清单检查失败。公开模板后续改为 Coding 地址和正式批准文件路径，机器清单仍记录旧摘要 | 阅读两份模板及现行边界，更新 `.env.example` 对应 currentSha256；历史 sha256、调用数、源码限额与证据断言保留 |
| 模板说明不一致 | PRO 注释仍使用旧名称；生产批准文件示例未充分说明容器不会自动得到宿主文件 | 注释统一为实际的 deepseek-flash；明确路径与只读挂载的区别。保留现有配置值、空密钥和默认关闭开关；测试扩大到 Vision 输入模式和两个模型超时参数的模板一致性 |
| 版本状态混淆 | 同一导航文档出现多段“最新/当前”，同时称质量待批准和已批准 | 当前状态统一为第九批次正式批准、生产尚未启用；早期暂停、失败和待批准记录明确归入历史阶段。实现地图保留历史内容并加当前说明，不改写旧比较结果 |
| 创建测试偶发超时 | jobs-create 用例第一次超过既有 20 秒限额；原日志未提供发生阶段 | 带计时探针复查通过，未改动的原用例再次通过。增加测试阶段诊断和超时后请求取消；20 秒限额与全部业务断言不变，修改后通过。未定位首轮超时根因，不将后续通过描述为消除了所有偶发超时 |

没有发现需要修改当前前后端业务逻辑的确定性失败。本轮业务实现保持不变，优化集中于可复核的发布状态、配置说明和回归诊断。没有删除测试、提高超时或构建告警门槛，也没有修改模型输出、参考答案或评分规则。

## 测试结果

最终工程检查通过：正式构建页面 307/307、全量单元 794/794、数据库/API/续跑 15/15。以下均为本轮执行，不引用上轮通过数充当本轮结果；首轮失败和超时仍列入发现及限制。

| 层面 | 结果 | 证据（本轮工件目录内） |
| --- | --- | --- |
| 全量单元 | 794/794，0 fail/skip/cancel | unit-final-snapshot.log |
| 发布检查 | 25/25；文档、模板、Gateway 与 Vision 清单 | release-final.log |
| 数据库、API、续跑 | 11 个文件、15 项最终均通过；创建首轮超时单独保留 | core-results.json、mongo-*.log、mongo-create-confirm.log、mongo-create-diagnostics.log |
| 路由 | 5/5 | routes.log |
| 生产构建与 Knowledge 备份 | 成功；入口 478.17 kB / gzip 159.05 kB，无 500 kB 提醒 | build.log、knowledge-backup.log |
| 正式构建全部页面 | 307/307，assetMode=production，约 9 分 17 秒 | ui/ui-results.json、ui.log、ui-results.json |
| Linux 部署生命周期 | 通过；安装、升级、持久化、备份、回滚、健康检查、停服备份、构建失败保护、迁移失败保护 | deploy.log、deploy-exit.txt（0） |
| Vision 离线 | 48 个原件、96 次模拟输出完成；qualityAccepted=false | vision-offline.json |
| 已批准真实质量复核 | 96 份输出重新评分，两侧各 48/48，当前真实配置准入 accepted=true | vision-approval-verification.json |
| 差异格式 | 通过 | diff-check.log |

首轮单元失败见 unit.log；修正过程中的文档摘要未同步失败见 interim-unit-final-snapshot.log、release-confirm.log，均保留而非覆盖。首次创建超时见 mongo-jobs-create.log，计时探针见 create-probe.log；探针未代替最终原用例复查。

源文件快照用于确认测试对象：source-before.json 为本轮开始状态；source-final-before.json / source-final-after.json 为全量单元测试前后状态（北京时间 18:29:31–18:29:42，1019 个文件摘要一致）。最终文档、清单摘要和测试诊断变化另有 source-after.json 收尾记录，随后发布检查通过；应用业务源码没有修改。测试工件不包含真实密钥或模型私有推理。

## 页面与性能

生产页面回归覆盖首页、手册、工作台、历史、详情、创建/恢复/导出/资料处理、错误提示、键盘交互，以及多个屏幕宽度。已人工查看本轮 320 px 首页和 1440 px 历史页截图，没有发现文字遮挡或横向溢出；完整页面质量以具体场景与截图为证，不宣称覆盖所有设备。

入口大小与上轮优化后的 478.17 kB 一致，历史页与平台说明的按需加载保持。本轮未修改前端资源，也未重新测量真实网络首屏耗时，不能声称再次提速。第三方依赖的构建提示仍可能出现；构建成功不等于所有第三方提示消失。

## 视觉批准与部署边界

已核对正式批准文件 `artifacts/vision-acceptance-glm53-fields-20260911-approved.json`，comparison 哈希为 `8d0bf01699843e4d98664682e6590619ae3eefe917dcb7732acb53585afa0895`。本次直接使用实际配置校验，没有替换服务地址；准入 accepted=true、reasons=[]。当前配置指向的文件与正式文件字节一致。

仅在验证进程的内存配置中开启开关，正确选中 candidate；缺失批准文件或关闭开关回落 legacy。实际配置保持 legacy。本轮没有创建新批准、修改已批准 comparison、启用生产路由或新增付费模型调用。原用户批准出处见 [正式批准记录](vision-operator-approval-20260911.md)。

现有生产 Compose 仅挂载 visual_attachments，不自动挂载批准文件；要启用候选，必须先加入明确的只读挂载，验证运行用户可读并核对部署代码/配置绑定。默认关闭开关时不依赖该文件。批准覆盖 48 份英文合成原件，不代表所有中文财报、未见复杂图表或长期零错误；V4.8.11 文本模型真实比较仍独立延期。

## 工程交付记录

| 项目 | 本轮结果 |
| --- | --- |
| 1. Release/subrelease | V4.9 回归维护，未进入 V5.0 |
| 2. 修改文件 | .env.example、.env.production.example、tests/harness.test.mjs、tests/jobs-create.integration.mjs、MANIFEST.json；docs/architecture 下两份现行地图；V4.9 的 README、DETAILED_INDEX、implementation、benchmark、vision-comparison-runbook、vision-call-inventory.md/json |
| 3. 新增文件 | 本报告及 artifacts/v49-regression-review2-20260911 下验证脚本、日志、截图与快照 |
| 4. 删除文件 | 无 |
| 5. 架构 | 无业务架构改动，复用现有 Gateway、视觉准入、研究恢复、测试及部署流程 |
| 6. Schema | 无持久化或 API 结构变化 |
| 7. 迁移 | 无新迁移；既有迁移/回滚流程已在隔离部署中复测 |
| 8. 环境 | 真实环境、依赖和脚本命令不改；公开模板仅说明文字变更 |
| 9. 兼容性 | 当前模型、证据和历史记录语义保持；模板校验更完整 |
| 10. Resume/recovery | 未改变时点/身份 pin/保存逻辑，Mongo 重启、密钥轮换、回滚暂停与取消重试用例复测 |
| 11. Feature flags | 未改变实际开关；内存隔离验证启用/关闭/文件缺失分支 |
| 12. 测试执行 | 见测试矩阵和新工件 |
| 13. 测试结果 | 见最终测试矩阵；首轮失败及探针记录全部保留 |
| 14. Benchmark | 新离线 48/96；旧已批准真实输出 96 份重评分；本轮新增真实模型调用 0 次 |
| 15. 安全/隐私 | API 合成数据、随机测试数据库、本地端口；Docker 临时项目清理；不暴露凭据、私有推理或改动金融事实 |
| 16. 回滚 | 按本轮文件增量撤回模板注释、测试诊断与文档，恢复相应当前摘要；不重置其他工作区改动，无数据回滚需求 |
| 17. 已知限制 | 首轮创建超时根因未证实；本地单浏览器测试不替代线上长期观测；正式候选部署挂载与激活尚未执行 |
| 18. 延期工作 | 未实现未来发布；不启动 V4.8.11 付费文本比较或生产候选激活 |
