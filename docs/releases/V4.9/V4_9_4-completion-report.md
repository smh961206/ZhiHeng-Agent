# V4.9.4 完成报告

冻结 48 个不同的合成财务视觉案例：16 表格 PNG、16 截图 PNG、16 纯扫描 PDF。覆盖六类指标／单位、两期间、显式负号／括号、空白／未读清／真实零、脚注和行列对应。全部为合成测试数据，不代表真实发行人事实。

新增 scripts/build-vision-fixtures.py、scripts/vision-benchmark.mjs（本阶段仅校验加载器）、tests/vision-benchmark-fixtures.test.mjs、tests/fixtures/vision-benchmark 下 48 个原件与 manifest.json、本报告。无运行时／架构／schema／迁移／环境／flags／API／恢复行为变更或删除；构建器仅开发使用现有 Pillow/reportlab，不增加生产依赖。

三个回归通过：48 个唯一原件哈希、三种媒体与关键案例覆盖；16 PDF 全部无 native 答案并通过生产渲染 worker；篡改和路径穿越被拒绝。查看全部联系表并用 Poppler 渲染检查 PDF 样本，版面可读，无裁切或遮盖答案。原件 SHA-256 固定，重建依赖字体／库版本；以已保存字节为准，不隐式重建验收语料。

无模型质量结果、付费或材料外发；48 个案例是冻结输入，不是 48 个模型通过结果。回滚移除本阶段新增开发资产即可，未改历史数据。限制：英文合成两行表格不能代表全部真实复杂中英年报；后续真实晋升还需要操作人质量复核，不能用模拟输出冒充模型性能。V4.9.5–.8 后续评分、候选和准入未在本阶段实施。
