# V4.9 历史模型批准工件

此目录只保存 V4.9 当时形成的 Vision 比较与人工批准证据，用于核对历史发布记录和旧任务状态。M1.0 schema v2 不读取此目录，也不以其中工件作为新任务启动条件。

`vision-acceptance-glm53-fields-20260911-approved.json` 从原根目录 `vision_acceptance_file/` 原样迁入；文件 SHA-256 保持 `4DA0DEED400395A16EB93C99E5C2C0E3D3C762D3789845A97207502C88F10118`。comparison 内容哈希仍以批准报告记录的值为准。
