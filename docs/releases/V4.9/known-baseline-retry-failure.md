# V4.9 最终回归：既有取消后重试失败

已修复：用户明确授权“都一并修复”后，index 与 research-retry 增加取消任务完成信号。重试等待保存确认及原控制器释放，期间保留互斥，超时或保存失败不开始新执行。真实 Mongo/进程测试通过 IPC 暂扣已可见记录的保存确认，证明第一次重试等待、第二次重试拒绝，放行后同一记录正常重试；不依赖固定延时或碰运气。见 [修复报告](V4_9-review-fixes-report.md)。以下为历史调查。

本轮验收追加：未修改运行时代码，重试诊断 2/2、完整 Mongo 集成 15/15 通过。本问题本次未复现，仍未修复；下文保留历史失败与调查，不据一次通过关闭问题。见 [本轮验收记录](V4_9-acceptance-report.md)。

日期：2026-09-11。状态：已复现，待单独维护；V4.9 全量验收不能标为通过。

最终源码在所有浏览器／部署检查结束后，以 `--test-concurrency=1` 运行全部 11 个 Mongo 集成文件：14/15 通过，0 skip/cancel。唯一失败为 `tests/jobs-retry.integration.mjs:66`：测试读到已取消的保存记录后立即发出第二次重试，期望 200，实际 409。候选模型、旧模型和研究生命周期的真实进程退出／恢复测试均通过。

从 HEAD `29a2785e0697fa9d76290efdbd7ec8d71856cc20` 导出 server/shared/knowledge/tests/scripts/package.json 到独立目录，使用同一隔离 Mongo 127.0.0.1:27029、同一 Node 和依赖，执行未修改的重试测试，得到同一第 66 行 409 != 200。两份测试仅工作区 CRLF 与 Git LF 换行不同，规范化后内容一致。证据：`artifacts/v4-9-integration-isolated.log`、`artifacts/v4-9-retry-baseline.log`；对照源码保留在 `artifacts/v49-baseline-source/`。

代码检查显示：`server/index.mjs` 在 `delivery.finish` 返回后才释放 controllers，而重试入口在 controller 尚在时返回 409；持久状态的可见时间与执行所有权的释放时间可能不同。这是与失败一致的时序解释，尚未用额外事件追踪证明每一次失败都来自该分支。V4.9 未修改这两个生命周期所有者，也未放宽原测试断言。

依 AGENTS.md 的发布边界，范围外改进须登记而不能顺便实现。本轮保留保护与全部断言，不把“基线也失败”当作验收通过。后续维护应明确取消完成与可重试的对外契约，保留持久保存、并发拒绝、重复请求去重及恢复保证；不得只增加等待、跳过测试或提前删除 controller 来掩盖问题。

数据库 ping 为毫秒级；创建集合／索引诊断分别约 478/177 ms，不能据此断言磁盘或沙箱是根因。早期并发超时和正常权限独立复测记录均保留，未用首次 15/15 的结果覆盖最终失败。
