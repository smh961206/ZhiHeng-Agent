# V4.9 表头、行标签与期间约束修正复验

最新批准状态：用户已明确批准本报告，正式批准文件与当前配置准入检查通过；生产路由仍关闭。见 [正式批准记录](vision-operator-approval-20260911.md)。下方待批准描述保留技术审核完成时点。

用户要求继续处理上一轮剩余的基线失败 VIS-044。本次明确区分 headers 与数据主体，禁止把表头创建为空数据行，同时按实际主体行数和期间列数核对数据单元格数量。该规则不固定行数、不硬编码任何原件答案，不删除真实空白单元格。

修改既有 scripts/vision-benchmark.mjs 的表格输出指令，并在 tests/vision-quality.test.mjs 加入已观察到的表头误作数据行、行名拼写不一致、行名混入脚注标记与日期期间对调回归。共用视觉系统指令、评分器、语料及历史结果不变。第四至第九批次独立执行，正式批准与生产路由保持关闭。

**最终第九批次：基线与候选均 48/48 严格通过，质量失败项为零。** 当前代码／配置绑定、原件审阅绑定与隔离回滚通过；正式操作人批准仍待确认，生产路由保持关闭。之前各批次及草稿保留历史，但指令改变后的旧绑定不适用于当前版本。

## 修正与审核方法

VIS-044 原图本轮再次查看：Metric、2022、2021 是表头；Operating cash flow 与 Capital expenditure 是两行数据，共四个数据单元格。之前基线额外生成的两个 Metric 空单元格仍由原评分器判错，不在评估后删除。两个模型收到同样的通用表头／数据主体约束；不向模型提供参考答案或固定单元格数量。

第四批次 artifacts/vision-live-glm53-headers-20260911-04.json 完成 96 次调用，基线 48/48、候选 47/48。候选 VIS-045 的 2021 单元格把 Nonrecurring gain 拼成 Nonreccurring gain，原评分器按缺失原行／额外错误行判失败；没有修正原始输出或忽略该错误。该批次报告哈希 b31a759762e56360cd4960a163cc9f2ba825aa1b14906814f8a10ec9f3a8925e，独立评估与逐项差异保留在 artifacts/vision-live-glm53-headers-20260911-assessment.json 和同前缀 case-review.md。

VIS-045 扫描原页本轮再次查看，确认原图拼写与参考一致。随后增加通用行标签约束：每行读取一次标签，跨期间复制完全相同的拼写，并逐字符对照原图；没有将这个具体行名或参考答案加入指令。第五批次 artifacts/vision-live-glm53-labels-20260911-05.json 从头比较所有 48 份原件，避免只复跑失败样本。

第五批次发现新的指令歧义：“原样复制标签”与“移除脚注标记”在模型执行上冲突，基线 VIS-014／016／017 把标记留在 row 中。主动停止后续调用，保留 37 条完成账本（36 份真实响应及一条 aborted 结果）；状态 interrupted，未把中断当成完整验收。最后一次请求已收到 HTTP 200，但中断导致响应正文未完成，模型身份回执与最终用量未知，费用也未知，不能自动重放。有效响应中基线 15/18 通过、候选 18/18 通过。

最终改为明确两步：先把脚注标记分离至 footnote，再将剩余行名写入 row 并在同行各期间复用。增加行名夹带脚注回归。第六批次 artifacts/vision-live-glm53-rows-20260911-06.json 在固定的新指令下重新覆盖完整语料；不拼接前几轮通过结果。

第六批次完成 96 次请求，候选 48/48，基线有效响应 47/47 严格通过；基线 VIS-022 请求遭遇 network 错误，HTTP 回执状态 unknown，是否计费未知。完整提取证据门槛 actual_extraction_identity_required 未满足，不能以其他批次结果补入原始 comparison。报告哈希 f0fe53b01fde913c8640e675ecaa7d5551ff4136958418a73f510c0de837d160。第七批次 artifacts/vision-live-glm53-confirm-20260911-07.json 保持相同指令与评分器，重新从头运行完整对照，以取得同一批次证据。

第七批次基线 VIS-002 数字、列标题正确，但把两列的期末日期年份互换；该错误继续由原评分器判失败。主动中断时共有 40 次请求，基线 19/20 份有效响应通过，候选 19/19 通过，最后一条 VIS-020/candidate 为 aborted，HTTP 回执 unknown，计费未知。原始报告哈希 679a94187e4e54177b8c5380561eb296e0bb743f905d9df777e1395423ad0618。此中断批次不能用于准入。

本轮重新查看 VIS-002 原图，确认日期应来自每列自身的年份与图中期末说明。新增通用约束：逐列确定日期、同列各行复用，并核对 date 与本列 heading；不将具体年份或日期答案写进指令。第八批次 artifacts/vision-live-glm53-periods-20260911-08.json 重新覆盖完整语料。

第八批次基线 VIS-018 仍把脚注标记留在 row；主动中断时 58 次请求，基线有效响应 28/29、候选 28/28 通过，末条 VIS-029/candidate 为 aborted，HTTP 回执 unknown。报告哈希 d6e898a7a5749ae2399946185eaa112f9997ea6968dceb040d384c8c2939e4b7。随后发现脚注指令的“复制到每个单元格”未限定字段，将其明确为只复制到 footnote、禁止包含在 row，从而与两步标签提取保持一致。第九批次 artifacts/vision-live-glm53-fields-20260911-09.json 在该固定指令下完整复验。

其余原件沿用上轮完成的视觉审阅，仅在每份原件、实际发送图像与上轮审阅绑定完全一致时复用。新响应单独重新评分并记录响应哈希。扫描 PDF 的审阅页还须匹配本轮实际发送图像，避免审阅不同渲染内容。

## 完整工程记录

| 项目 | 结果 |
|---|---|
| 1. Release | V4.9 表头、行标签与期间输出约束修正及真实复验 |
| 2. 修改文件 | scripts/vision-benchmark.mjs、tests/vision-quality.test.mjs；Vision inventory、V4.9 README／DETAILED_INDEX／runbook／benchmark、MANIFEST.json 随结论同步 |
| 3. 新增文件 | 本报告；本地第四至第九批次运行／评估／审核脚本、原始结果、HTTP 回执、逐项审核和待批准草稿 |
| 4. 删除文件 | 无生产或历史文件删除；隔离检查临时见证完成后清理 |
| 5. 架构变化 | 扩展现有 benchmark 指令，无新模块、无 Gateway 边界变化 |
| 6. Schema | 无持久 schema 改动 |
| 7. 迁移 | 无迁移、无历史回填 |
| 8. 环境 | 真实 .env、生产配置、密钥和示例未在本轮更改；仅测试进程覆盖候选 Coding 地址 |
| 9. 兼容 | 接口结构不变；新指令改变比较代码绑定，旧批准草稿不能用于当前版本 |
| 10. Resume／Recovery | 独立批次保留每次请求账本，不重放旧批次；既有 Mongo 重启通过记录沿用，当前结果另做隔离准入／回滚检查 |
| 11. Feature flags | FEATURE_VISION_ROUTING 保持关闭；没有安装正式批准文件 |
| 12. 测试执行 | 修改前基线、针对性、全量单元、完整真实比较、独立重评分、同图审阅绑定、隔离回滚、指纹和文档校验 |
| 13. 测试结果 | 修改前 8/8；前四次约束修正各阶段针对性 16/16、全量单元 793/793；最终字段归属修正针对性 16/16、全量 794/794；最终文档与指纹检查见下方结果 |
| 14. Benchmark | 第四批次基线 48/48、候选 47/48；第五／第七／第八批次主动中断；第六批次已返回 95 份全部通过，但一例网络失败；第九批次两侧均 48/48；本轮六批次合计 423 次 HTTP，全部历史原件和输出完整保留 |
| 15. 安全／隐私 | 仅冻结合成图与指令外发；不发送参考答案，不保存密钥、原图 base64 或隐藏推理；不伪造操作人批准 |
| 16. 回滚路径 | 保持 flag=false；以后回滚用关闭开关或 start:legacy，保留旧记录；candidate pin 暂停，不能静默换模型继续 |
| 17. 限制 | 48 份英文合成表格的单轮结果，不代表真实复杂财报或长期稳定性；账单金额未知 |
| 18. 待办／延期 | 操作人审核后才形成正式批准；生产启用、扩展真实中文复杂样本、未来版本功能不自动执行 |

## 最终真实结果与技术审核

| 项目 | deepseek-flash | glm-5.3-flash |
|---|---|---|
| 真实响应／请求 | 48/48 | 48/48 |
| 严格通过 | 48/48 | 48/48 |
| 九项逐单元格指标 | 各 192/192 | 各 192/192 |
| 平均／中位／P95 耗时 | 1.675／1.586／2.365 s | 7.945／7.636／11.860 s |
| 输入／输出／总 tokens | 90,048／8,696／98,744 | 148,672／27,554／176,226 |
| 实际账单金额 | 未知 | 未知 |

最终批次 96 次 HTTP 全部返回 200，无传输重试、失败或未知请求；服务端 model 字段与请求模型一致。第四至第九批次本轮合计 423 次真实 HTTP 尝试，全部记录保留，不以最终批次覆盖中间失败。第五批次最后一次虽收到 HTTP 200，但中断使正文未完成，模型回执身份与最终用量未知；第六批次还有一次网络失败，没有收到 HTTP 响应。第七批次主动中断的请求也没有收到响应。第八批次也保留一次 aborted 及 unknown 回执。四次受影响请求均不自动重放，费用未知。供应商回执不是独立签名或账单。

48 份原件、两侧实际图像、96 条完成账本、结果哈希和进度校验和全部复核。VIS-002／044／045 本轮重新查看，其余原件复用与当前图像完全匹配的上轮视觉审阅。16 份 PDF 原页与实际发送图像哈希一致。第九批次的 96 份响应均独立重新评分，表头、多余单元格、拼写／单元格归属、脚注和缺失值检查全部通过；没有改变评分器或事后修正模型原文。

以最终结果在隔离进程检验：测试专用见证可选择 candidate，关闭开关回到 legacy；旧 pin 可用，candidate pin 暂停且状态不被替换。临时见证已清理，从未作为正式批准。此前真实 Mongo／子进程重启 1/1 通过的证据沿用，本轮未更改恢复代码，未再次运行该集成测试。

正式批准草稿 artifacts/vision-acceptance-glm53-fields-20260911-draft.json 保留原始 comparison；approvedBy、approvedAt、visualReviewPassed 均为 null，rollbackVerified=true。准入唯一剩余项为 operator_review_required。[操作说明](vision-comparison-runbook.md) 要求“可信操作人的文件见证”；技术审核不替代操作人的确认。候选仅在比较进程使用 https://open.bigmodel.cn/api/coding/paas/v4，生产正式配置必须与报告绑定一致。

报告哈希：8d0bf01699843e4d98664682e6590619ae3eefe917dcb7732acb53585afa0895。

## 验证证据

- artifacts/vision-live-glm53-fields-20260911-09.json：最终原始实测输出。
- artifacts/vision-live-glm53-fields-20260911-transport.jsonl：最终 96 次请求及响应回执。
- artifacts/vision-live-glm53-fields-20260911-assessment.json 与同前缀 case-review.md：独立重评分、48 例逐项结果。
- artifacts/vision-fields-technical-review-20260911.json：原件／响应绑定、完整性及隔离回滚结论。
- artifacts/v4-9-header-constraints-baseline.log：8/8；同前缀 focused.log：16/16；同前缀 unit.log：793/793。
- artifacts/v4-9-field-constraints-focused.log：16/16；同前缀 unit.log：794/794，0 fail／skip／cancel。
- artifacts/v4-9-output-constraints-recovery.log：此前真实 Mongo／子进程重启 1/1，通过记录沿用。
- artifacts/v4-9-field-constraints-final-checks.log：最终文档／清单及 Vision 所有者指纹检查。
- artifacts/vision-fields-final-audit-20260911.json：六批次历史哈希保持不变、423 次物理尝试核对、最终代码绑定／锁释放、临时见证清理及敏感内容检查。最终文档与 inventory 测试 14/14 通过，git diff --check 通过；真实环境路由开关仍为 false。

本次是开发中已知失败模式修正后的冻结合成语料复验，不是独立未见样本测试。中间批次的偶发拼写错误表明单轮通过不能证明长期零错误；复杂财报、中文与真实业务覆盖仍需另行扩展。
