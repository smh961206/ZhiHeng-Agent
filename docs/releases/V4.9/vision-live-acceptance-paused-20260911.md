# V4.9 真实模型质量验收暂停记录

后续：用户已明确恢复真实验收，见 [独立新批次记录](vision-live-coding-acceptance-20260911.md)。本文件保留前一轮暂停时点与原始失败事实。

日期：2026-09-11。状态：**用户主动搁置，等待用户明确恢复**。用户指示：“模型质量验收暂时先搁置，并且记录一下”。不安排自动重试或付费调用，不生成批准文件，不启用候选路由。V4.9 工程与离线验证完成状态不等于真实模型质量验收通过。

## 本轮事实

- 使用本地 .env：基线 deepseek-flash，候选 glm-5.3-flash／zai，候选地址 https://open.bigmodel.cn/api/paas/v4，INPUT=images、THINKING=omit。密钥未写入报告。生产环境候选密钥未配置、INPUT=off；两侧环境 FEATURE_VISION_ROUTING 均关闭。
- 原计划 48 份冻结合成原件、96 次比较调用。原始比较账本登记 49 次：基线 24 次完成、候选 24 次失败、另 1 次基线 VIS-025/baseline 已登记但结果不明。停止后没有自动重放或跳过该请求。
- 智谱 24 次比较均被网关归类为 rate_limit，无有效候选输出。随后仅追加 1 次独立诊断，返回 HTTP 429、供应商错误码 1113、消息“余额不足或无可用资源包,请充值。”。比较登记与诊断合计 50 次，未继续用完原计划额度。账户额度失败不能解释为模型质量失败。
- 基线已完成的 24 例中，21 例通过现有严格评分，VIS-009、VIS-014、VIS-018 的 footnote／relationship 未通过：输出 footnotes 额外包含图片中的通用说明。参考答案与评分门槛保持原样；这只是部分样本结果，不代表完整验收结论。
- 基线 24 个已返回响应共报告输入 30,768 tokens、输出 4,546 tokens，总计 35,314 tokens。其余用量未知；所有真实账单金额均未知，不按零费用记录。
- 因连续失败而停止已确认身份的本轮进程。原始 JSON 最后持久状态仍为 running、completed=false，并保留 .lock 和 reserved；这是被停止时的现场，不表示仍有运行任务。本记录和独立 assessment 表明已停止并暂停，不手工改写带校验和的原始账本。

## 证据位置

以下是工作区本地 artifacts，不包含密钥或图片 base64，不作为已批准产物：

- artifacts/vision-live-glm53-20260911-01.json：原始比较与调用账本。
- artifacts/vision-live-glm53-20260911-01.json.lock：中断现场锁，保留核对。
- artifacts/vision-live-glm53-20260911-01.log：原执行日志。
- artifacts/vision-live-glm53-diagnostic-20260911.json：单次诊断，包含脱敏供应商错误码和消息。
- artifacts/vision-live-glm53-20260911-assessment.json：独立部分结果统计与未知请求列表；生成时配置／代码绑定仍匹配。
- artifacts/v4-9-live-preflight-20260911.log：执行前相关回归 14/14 通过。

## 恢复条件

1. 用户明确要求恢复真实验收，并确认新的调用安排；旧的未用额度不视为自动续跑授权。
2. 智谱账户恢复可用 API 余额／资源，并确认相应模型可用。恢复时先做有界连通性诊断，不能盲目整轮重试。
3. 核对 VIS-025/baseline 的供应商记录和可能计费情况。当前账本含结果不明请求，现有 --resume 会拒绝；已记录失败也不会自动补跑。不要修改或拼接旧账本冒充完整运行，需要重新安排独立运行时保留旧结果。
4. 再核对语料／模型配置／代码绑定；完整真实对照与逐图审核通过之前，qualityAccepted 不成立。生产部署仍需独立配置与审核。

## 工程记录

| 项目 | 结果 |
|---|---|
| 1. 发布范围 | V4.9 真实验收执行与用户暂停记录 |
| 2. 修改文件 | V4.9 README、DETAILED_INDEX、vision-comparison-runbook、MANIFEST.json |
| 3. 新增文件 | 本报告及上述本地验收／诊断工件；独立诊断脚本位于 artifacts |
| 4. 删除文件 | 无 |
| 5. 架构变化 | 无；比较与诊断复用现有渲染／Gateway |
| 6. Schema | 无 |
| 7. 迁移 | 无 |
| 8. 环境变更 | 本轮没有修改环境或密钥；读取用户已配置的本地 .env |
| 9. 兼容影响 | 无运行时代码修改，无生产切换 |
| 10. Resume／Recovery | 保留未知请求与原始记录；不绕过续跑保护 |
| 11. Feature flags | FEATURE_VISION_ROUTING 保持关闭 |
| 12. 测试 | 验收前 challenger／policy／fixtures／resume 相关回归；文档工程检查 |
| 13. 结果 | 验收前 14/14；文档检查另记本地日志 |
| 14. Benchmark | 部分真实结果如上，完整质量验收未完成 |
| 15. 安全／隐私 | 仅固定合成图像和读取指令外发；无真实发行人材料、参考答案、密钥或私有推理外泄 |
| 16. 回滚 | 无生产变更需回滚；保持关闭并保留证据 |
| 17. 限制 | 候选无有效输出、基线样本不完整、存在一次结果不明调用、费用未知 |
| 18. 延期 | 真实模型质量验收及生产晋升按用户要求暂停，等待明确恢复 |
