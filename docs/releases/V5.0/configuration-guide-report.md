# V5.0 配置使用教程交付报告

1. **版本**：V5.0。
2. **修改文件**：README、生产部署说明、V5.0 详细索引及发布清单。
3. **新增文件**：`docs/configuration-guide.md` 与本报告。
4. **移除文件**：无。
5. **架构变化**：无；文档明确单环境文件与独立模型 JSON 的既有边界。
6. **Schema 变化**：无。
7. **迁移**：无新增迁移；教程说明旧变量环境的迁移方法。
8. **环境变化**：无；记录现行本地和生产配置入口。
9. **兼容影响**：无；旧环境变量入口仍按当前代码兼容。
10. **恢复影响**：无；教程明确环境文件与模型 JSON 成对恢复。
11. **功能开关**：无变更；教程保留所有准入与发布门槛。
12. **测试**：Markdown 链接、清单哈希、配置示例及完整单元测试。
13. **测试结果**：完整单元测试 888/888 通过；文档相对链接与发布清单校验通过；本地 6 个模型档案配置等价且模型调用为 0；生产 Compose 配置解析通过。
14. **Benchmark**：不适用，模型调用为 0。
15. **安全与隐私**：示例不含凭据，教程禁止将密钥写入 JSON、前端变量或版本库。
16. **回滚**：移除新增文档入口即可；不影响运行配置。
17. **已知限制**：静态配置检查不证明接口连通、模型能力或生产质量准入。
18. **延期工作**：真实模型验收和生产策略启用仍按独立流程执行。
