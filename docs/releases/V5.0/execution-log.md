# V5.0 execution evidence

## 2026-09-11 latest decision — live acceptance paused by user

用户明确要求暂停本轮真实验收。暂停真实模型对照、付费测试、人工质量验收和生产候选启用，等待用户明确恢复；不自动续跑或启用策略。既有工程实现、模拟基准和回归证据完整保留，真实质量与生产晋升仍未验收，CURRENT 保持 V5.0。此记录仅修改本日志、release README、acceptance、completion report 及其 MANIFEST 校验值，无新建／删除文件、运行代码、schema、迁移、环境、恢复逻辑或开关变更；既有验证结果不重写。

User authorization on 2026-09-11 expands CURRENT from V4.9 through the entire V5.0 sequence. CURRENT is authorization, not quality acceptance. The normalized .0–.13 order overrides the historical five-part overview. The stale `server/model-gateway/` inspection path resolves to existing `server/model-gateway.mjs`, Catalog, adapter, state and rollout owners.

Prerequisite review: V4.9 engineering and operator-approved Vision quality exist; V4.8 live text-policy acceptance remains deferred. V5.0 offline engineering can reuse the existing legacy baseline, but cannot imply acceptance of that text policy or activate unmeasured models. Production promotion will remain closed until the required concrete benchmark evidence and human approval exist (ADR-011/014).

Baseline: 129/129 affected Catalog/Gateway/state/rollout/text-comparison/Vision-quality tests passed, no skips. Evidence: `artifacts/v5-baseline-tests.log` (local ignored artifact).

## V5.0.0

New owner `benchmark/case.mjs` defines versioned immutable evaluation metadata, category taxonomy, explicit financial basis and source lineage, missing values, required capabilities/validations and forbidden behavior codes. Existing regression/visual fixtures are preserved. No runtime, API, investment schema, migration, environment, recovery or production routing change. Rollback: stop consuming these additive benchmark files. New tests: `tests/benchmark-case.test.mjs`. Live quality gate is not applicable to this contract-only step.

Result: 2/2 tests pass. Modified CURRENT; new case module/test/log; no removed files. No feature flag, benchmark measurement or security exposure added.

## V5.0.1

New `benchmark/fixtures.mjs` validates versioned manifests, SHA-256 assets, data-only JSON, case/fixture linkage and source publication/market timestamps against each cutoff. Paths cannot escape the fixture root, including resolved symlinks. Unknown data is retained; no acquisition is performed. New `tests/benchmark-fixtures.test.mjs`; combined .0/.1 tests 4/4 pass. Local benchmark artifact schema v1 is additive, outside Mongo/research records. No migrations, public API, production environment, feature flags, recovery or investment semantics change. Rollback: retain and stop reading new fixtures. Champion/quality measurement not yet applicable.

## V5.0.2

New `benchmark/graders.mjs` grades financial values/basis/source lineage, source quotes, tool behavior, validation receipts, counter-evidence, delivery and forbidden behavior; it reuses `server/vision-quality.mjs` without changing Vision grading. No score can compensate for a hard failure. Strict evaluation projection excludes private provider fields. New `tests/benchmark-graders.test.mjs`; 18/18 benchmark, existing citation and Vision tests pass. No removed files, production schema/API/environment/model calls or migration. Benchmark records are evaluation observations, never verified financial facts. Rollback and future-work boundaries remain as above; semantic policy is the next step.

Independent review before runner integration found and fixed empty-hash bypass, insufficient nested output types, ignored extra failed validations, unbound grader fixtures, invalid dimension success defaults, noncanonical cutoff dates and missing Vision references. Hard receipts must be supplied by the trusted executor, never the evaluated model. Full pre-runner existing unit suite passed 800/800; subsequent corrective tests are recorded in final acceptance.

## V5.0.3

New `benchmark/semantic.mjs`, `benchmark/contracts/semantic-policy.md` and `tests/benchmark-semantic.test.mjs` implement versioned expression/argument rubrics with candidate/model-version, case, output, fixture and suite hash binding. All semantic observations remain non-accepting; hard grades are recomputed and cannot be overwritten. No automatic grader call or hidden-reasoning dependency. Nine new tests pass alongside existing .0–.2 tests. No removed files, runtime changes, persistent research schema, migrations, environment or feature flags. Rollback: remove consumers of the additive policy; observations remain audit evidence. Live quality/promotion remains inapplicable here.

## V5.0.4

New `benchmark/runner.mjs` reuses existing comparison atomic writes, Catalog validation, ModelCall normalization and Vision grader. It binds runs to frozen suite/config/code/grader identities, reserves each unit durably, stores whitelist-only results and aggregates known/unknown metrics. Completed units are not replayed; unknown dispatch outcomes fail closed. A single local lock prevents concurrent writers; no distributed scheduler is added. New `tests/benchmark-runner.test.mjs` and shared test fixture helper; .0–.4 suite 20/20 passes. Local run/result schema v1 is additive, with no Mongo/API/research migration. Production calls/routing remain unchanged; importing the runner never executes it. Rollback: stop runner, preserve all artifacts. Frozen baseline is the next step.

## V5.0.5

New `benchmark/baseline.mjs` freezes a complete single-profile run with policy identity, exact model/grader/executor/code/suite versions, runtime environment and metrics. Exclusive creation prevents overwriting a baseline. `qualityAccepted` stays false: simulation measures engineering, not live model quality. New `tests/benchmark-baseline.test.mjs`; baseline/runner 5/5 tests pass. No deleted files, API, production state/environment/feature flags, migrations or recovery changes. Local baseline schema v1 is additive. Rollback: restore an earlier explicitly selected baseline while retaining the newer artifact. Initial corpus execution is completed with .7 after the challenger adapter is available; real text-model quality acceptance remains pending independently.

Bootstrap import adds 8 explicitly synthetic missing-data text cases and 48 byte-identical existing Vision originals under `benchmark/fixtures/bootstrap-v1`, with reproducible importer `scripts/build-benchmark-fixtures.mjs`. These are component tests, not full research quality samples. The initial text reference selfcheck passes 8/8. A Windows atomic-rename lock interrupted the Vision selfcheck; the incomplete run is preserved under `artifacts/v5-baseline-step5`. The runner now retries only transient file writes, bounded to five attempts; no model request is replayed. Final corpus evidence is regenerated after all code is fixed. Independent runner/baseline review found no P1 blocker after provenance hardening.

## V5.0.6

Existing Catalog/connection/adapter/Gateway now support explicit ModelProfile v4 `main-challenger`, declared text capabilities and omit/disabled/enabled thinking mapping. `createBenchmarkModelCatalog` is an opt-in catalog; old default/policy factories and modelState v1/v2 remain unchanged. New `tests/model-challenger.test.mjs` tests mock wire, capability refusal, errors and cancellation. Subagent affected tests: 248 passed; 3 reviewed Vision fingerprint failures were corrected by updating current hashes only, retaining historical hashes. Production defaults and existing Vision constraints are unchanged. New configuration fields are opt-in; no live calls, Mongo migration, public API or resumed-task changes. Rollback: omit challenger configuration/custom catalog. Approval and production usage are later steps.

Post-inventory verification: 56/56 Catalog/Gateway/challenger/Vision inventory tests pass.

## V5.0.7

New `benchmark/executor.mjs` and `scripts/benchmark.mjs` perform explicitly isolated component extraction comparisons through the existing Gateway and canonical Vision request/renderer. The local executor owns extraction-contract receipts, not the model. Text simulation uses two MAIN profiles across 8 frozen cases, twice each: 32/32 graded outputs pass with actual mock Gateway receipts, no network and unknown costs preserved. New `tests/benchmark-executor.test.mjs` verifies this plus completed-run resume and live authorization rejection. Live runs require explicit paid authorization, both profile credentials and durable request/budget reservations reused from the existing comparison owner. No background/shadow production calls, investment-schema or Mongo changes. Runner gains connection/configuration identity binding. Rollback: stop explicit CLI and retain run/budget artifacts. Component samples and mock metrics do not certify complete research or live quality; statistical admission follows.

## V5.0.8

New `benchmark/statistics.mjs` uses conservative 95% Wilson interval differences, a minimum 50 distinct frozen fixtures per task, at least two paired repeats, >=95% case pass rate, <=10 percentage point noninferiority tolerance and zero candidate hard failures. Repeats do not inflate the independent sample count; duplicate/unpaired inputs fail, simulation cannot qualify, and cost is not used to override quality. These explicit conservative engineering thresholds are versioned, not empirically calibrated guarantees. New `tests/benchmark-statistics.test.mjs`; statistical/executor 4/4 tests pass. No runtime policy, schema/migration/API/environment/flags/recovery change. Rollback: use the earlier comparison policy while retaining versioned evidence. Bootstrap samples are intentionally insufficient for production promotion.

## V5.0.9

New `server/model-task-class.mjs` defines the six task classes and a versioned pure classifier over known purpose/mode/document/tool-workflow signals. Missing information remains unclassified, Mode A retains its quick-screen cap, and availability/data failures do not alter class. Benchmark mapping shares the same known-signal path and cannot infer long documents from a category name alone. New `tests/model-task-class.test.mjs`; class/statistical tests 4/4 pass. Job pinning is the next schema step, not silently added here. No production caller/API/schema/migration/flags/recovery change; rollback removes classifier consumption while retaining recorded versioned classifications.

## V5.0.10

New `server/model-champion.mjs` validates versioned task champion policies against explicit MAIN capabilities, recomputed sample/statistical gates, code/configuration identity and an artifact-bound operator attestation including pipeline, dry-run and rollback review. `benchmark/champions.mjs` stores an append-only set of policy versions with a movable active pointer; old policies cannot be overwritten or removed. Unsupported tasks and disabled selections retain default behavior. New `tests/model-champion.test.mjs` plus test-only synthetic attestation helper; champion/class/statistical 6/6 tests pass. No genuine approval or active production registry was created. New local registry/policy schema v1 is additive, not a research fact or Mongo migration. No public API, automatic cost selection, environment activation or recovery change. Rollback: clear activePolicyId while retaining policies; runtime integration follows.

Independent review identified approval transplantation; evidence now binds measured configuration/code/classifier/effort and research-pipeline scope, while approval separately binds rollout ID/stage/percentage/task scope/settings. Old approvals cannot authorize a new model, endpoint, effort or percentage; regression tests and independent recheck confirm the fix. A separate executor review fixed missing-budget resume (now refuses) and enforces operator timeout with a cleaned-up timer; both have regression tests.

## V5.0.11

New `server/model-experiment.mjs` deterministically assigns a job to baseline/candidate using a versioned SHA-256 bucket. Existing `server/model-state.mjs` adds explicit private v3 with immutable policy/group/configuration/job-time/mode bindings, while v1/v2 remain exact. Existing Gateway consumes a single fixed MAIN profile for research/review/followup and verifies the actual custom Catalog; v3 never enters v2 escalation. Public routing labels show actual pinned analysis identity without exposing private metadata. New `tests/model-experiment.test.mjs` 12/12 and `tests/model-experiment.integration.mjs` 2/2 pass independently in the parent. Real Mongo tests use random disposable databases on loopback port 27029 and cover both groups, process exit/restart, key rotation and configuration drift before pending work. Source cutoff, private messages and completed tools are retained. No database/index migration or historical rewrite; see schema/migration/rollback docs for the explicit additive v3 boundary. Production creation remains gated until .12.

## V5.0.12

Existing `model-rollout.mjs` and `research-create.mjs` admit and pin new jobs only with champion routing mode, explicit enable switch, matching registry/policy version, complete quality/operator binding and credentials. A/B additionally requires its separate opt-in switch. Disabled/dry-run/unmatched tasks retain legacy; old approved policy versions may resume without reassigning group, while rollback pauses all v3 jobs before execution. Actual in-flight HTTP calls are not forcibly cancelled by a configuration file change. Final hardening additionally rechecks approval before every subsequent v3 Gateway call; the original job start/retry check remains. Existing telemetry recognizes champion mode, with detailed selection lineage remaining private in the referenced job. Docker now copies the benchmark runtime helpers and dependency lockfile; its isolated deployment fixture includes benchmark too. Example environments add empty challenger credentials and disabled controls. No production environment was edited or activated. New `tests/model-champion-rollout.test.mjs` plus existing rollout/create/telemetry tests: 85/85 pass; strengthened explicit candidate review format capability and revised drift mutations: 18/18 state/champion/rollout tests pass. No Mongo migration, old-record rewrite or extra background calls. Public version label is 5.0; all 307 existing UI scenarios pass after version expectations were aligned with the public version source.

## V5.0.13

New `server/model-drift.mjs` and `scripts/model-drift.mjs` compare explicitly supplied periodic results against an approved baseline. Complete acknowledged runs are strictly verified against the frozen suite using the runner's shared validator; row identity/provenance and deterministic grades are recomputed. Baseline plan binding, run ID and exact result hash must match approved evidence; a same-plan rerun cannot replace the accepted baseline. Every approved task class requires coverage. Valid live critical/format/tool regressions or changed execution code/configuration emit invalidation; insufficient evidence stays inconclusive, and simulation cannot create a live invalidation. The alert never chooses another model. The optional invalidation file closes only the exact matching policy. No scheduled paid run, registry mutation or production alert was enabled. New drift tests cover source relabeling, fabricated grades, altered/replayed baselines, dropped tasks, incomplete acknowledgements and CLI rejection. Local alert schema v1 is additive; no Mongo migration, investment schema, public API or destructive rollback. Retain alerts and original job pins; restore compatible approved configuration before resuming.

## Final independent review and regression

Independent review found incomplete approval code/configuration binding, insufficient baseline/row verification and missing per-call revocation checks. All were addressed before final regression. Approval now binds server/shared/benchmark code, active Knowledge and dependency manifests plus review format/effective deadlines. Key rotation remains compatible. Policy evidence includes run IDs and output hashes as well as plan bindings. Revocation/invalidation between two turns blocks the second wire while preserving checkpoint and selection. A/B fixtures use genuine temporary test registries instead of bypassing the guard. Targeted final hardening: 20/20; both real Mongo A/B restart cases pass after those changes.

Final unit regression: 868/868, zero failures, skips or cancellations. Twelve Mongo groups: 17/17 tests. UI: 307/307 scenarios; routes: 5/5. Final mock Gateway comparisons: text 32/32 and Vision 192/192. Two immutable baseline-arm replay snapshots pass 16/16 and 96/96 respectively, with original comparison result hashes and explicit simulation provenance; these replays add no model calls. Statistical comparison correctly rejects promotion for every task due to simulation and/or insufficient independent samples. Full validation, deployment outcome and artifact identities are in [completion report](completion-report.md) and [validation records](validation-results.json).

Final classification audit found explicit long-document/review signals were not carried from frozen cases into stored results. Optional typed taskSignals are now copied only from the frozen case and strictly revalidated against its hash; executors and edited result files cannot redefine the class. The new test exercises both supported classes and tampering/type rejection. No historical field is backfilled, and cases without signals retain their original shape. This scoped fix prompted the final 868-test regression and regenerated final comparisons/baselines. The nine-group deployment regression passed; its final migration/build failure injections were confined to the disposable test copy. Final image packaging is separately rebuilt after this benchmark-only fix.
