# V5.1 execution evidence

User authorized the whole V5.1 release on 2026-09-12. CURRENT selects V5.1; V5.0 real-model acceptance remains explicitly paused. Production promotion and paid experiments are not resumed by this engineering activation.

Normalized .0–.14 order applies. The historical overview's six groupings are not execution order. The template path server/model-gateway/ is stale: existing owners are model-catalog.mjs, model-gateway.mjs, model-gateway-result.mjs, model-telemetry.mjs and storage.mjs. Local benchmark owners remain in benchmark/.

Baseline: 53/53 existing catalog, Gateway and telemetry tests passed. No existing uncommitted changes were present.

V5.1.0: dated immutable pricing added to the existing Catalog; old pricing retained. 55/55 affected tests passed, including accessor/type safety and unknown/zero distinction. No routing behavior or benchmark quality change. Initial regression caught boxed currency acceptance; fixed the validator without changing the old test.

V5.1.1: configuration-only registry resolves non-overlapping half-open effective windows, version, known-at date and exact profile/connection identity. Missing configuration gives unknown; invalid configuration fails validation. Administrative history is read-only; no automated price acquisition or historical rewrite.

V5.1.1 validation: 56/56. V5.1.2: existing adapter normalizes reasoning as an output subset, retains provider/estimated/unknown sources and the legacy four-field usage facade; 58/58. V5.1.3: Gateway pins call-time pricing before transport, calculates token-cost-1 and stores a reproducible nullable receipt through existing ModelCall storage; 59/59. Invalid prices cannot fail research or become zero. V5.1.4: existing telemetry aggregates purpose/profile and deduplicates call lifecycle receipts, counts actual dispatch attempts, explicitly leaves prior failed transport costs unknown; 61/61. No new store, financial arithmetic, model admission or tool execution.

V5.1.5: effective-task-cost-1 = all recorded model-call costs across all tasks / number of validated deliveries with zero critical errors. Failed tasks and review/tool-loop calls remain in the numerator. Unknown call costs, attempts or mixed currencies prevent a complete value. Tool rounds are reported separately, never invented as paid cost. Existing benchmark statistics exposes the comparison without quality approval or champion mutation.

V5.1.5 validation: 25/25 scoped tests. V5.1.6: stable leading system/configuration SHA-256 fingerprint at Gateway, no prompt/assistant/image body persisted; 41/41 affected tests. V5.1.7: exact profile/fingerprint groups, deduplicated successful provider observations, token-weighted and call hit ratios, missing coverage and seven-day past window; 39/39 affected tests. V5.1.8: cache-eligibility-1 requires text prefix, at least 30 observed calls, five distinct jobs, 90% known coverage and 50% observed token ratio. These conservative thresholds are engineering eligibility rules, not measured production savings or quality approval. No provider switch is introduced.

V5.1.8 validation: 40/40. V5.1.9: existing Champion owner emits read-only, currency-separated effective-cost rankings after its actual admission validator and health/capability checks; 25/25. Legacy shadow policy retains its pure boundary. V5.1.10: immutable explicit budget limit schema, no enforcement yet; 6/6 with existing resume tests. V5.1.11: existing job payload receives private budgetState, mandatory pre-dispatch reservation and completion saves, serialized writes, call/round identity, model/review/web/Vision accounting; 68/68 affected tests. Unknown or replayed requests pause rather than execute twice. Public job/list projections exclude the private ledger.

V5.1.12: opt-in enforcement checks configured resource limits before new operations; price/context/output uncertainty blocks monetary authorization. Default configured budgets are dry-run; rollback disables enforcement while retaining accounting. Near 80% pressure, only exact repeated local evidence retrieval with unchanged source snapshot/arguments can reuse its prior result. Unknown-priority and critical work are never heuristically removed. If no capacity remains, research stops with unresolved-validation disclosure rather than bypassing review. Duration is checked at resource boundaries; transport retains its own bounded deadline. This is a planning limit for estimated provider model fees, not an invoice guarantee or a whole-machine kill timer.

The normalized templates say no persistent schema change, while .1/.4/.11 explicitly require historical pricing, cost receipts and durable budget usage. The release schema requires additive telemetry. Use additive versioned receipts in existing stores, preserve unknown historical values, and document compatibility/rollback before integration. No financial schema changes or backfill.

V5.1.12 validation: 64/64 affected tests, plus actual Agent interruption/continuation coverage. V5.1.13: v2 daily timezone/tier schedules, exact call-time resolution and retained schedule snapshots; 30/30 scoped tests including midnight and DST. No scheduling delay or model substitution.

V5.1.14: safe GET cost endpoint and lazy-loaded desktop/mobile research-process panel; real Mongo/API isolation, missing/zero/tiny-positive/multi-currency costs, errors and private-field exclusion covered. All 332 existing and new UI scenarios pass against the final production build; the six new cost-panel cases also have inspected screenshots. The first full UI run exposed reuse of an existing document-summary CSS class; giving the cost panel its own class restored all legacy selectors without weakening tests.

Final engineering validation: 911/911 unit tests; 125/125 release tests; 18/18 Mongo/API/process-restart tests across 13 groups; five route renders; 332/332 UI scenarios; 32 text and 192 Vision offline attempts; six frozen cost/quality cases; production build, isolated Linux deploy/upgrade/persistence/backup/rollback/failure suite and final-image offline module smoke. Three database tests timed out while other heavyweight checks were running; isolated unchanged reruns passed all four assertions in those three files with zero cancellations. Exit codes alone were not counted as acceptance. The first container deployment attempt had an outer-daemon bind-path mismatch; the same suite passed after using an explicitly shared temporary directory.

Final approval review binds the budget configuration and cost-related code owners to the existing quality approval fingerprint. Flags remain off, actual environment files and production services are unchanged. Source inventory current hashes were refreshed without altering historical baseline hashes. Complete file accounting, limitations, exact artifact hashes and deferred work are in completion-report.md and validation-results.json.
