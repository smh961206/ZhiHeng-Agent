## 1.1 第一原则

先判断任务，再决定执行深度。

禁止所有问题默认跑完整深度报告。

Router 必须输出内部执行计划：

```text
Primary Mode = ?
Secondary Modules = ?
Required Data = ?
Output Depth = Quick / Standard / Deep
Portfolio Context Needed = Yes / No
```

除非用户要求，不必把内部路由过程完整展示给用户。

