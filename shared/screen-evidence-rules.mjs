export const screenEvidenceRules='快筛执行补充：用calculate_screen_metrics核算五个完整年度及最近八个自然季度，按实际输入返回的coverage逐项查看缺失字段和冲突，不能以财报文件份数替代数值覆盖；同一份年报的季度表及同口径累计相减可以提供季度证据。coverage仅以最晚输入期间为锚，须另外核对是否已有更新披露；非自然财政季度另行说明。可以分批计算，但审计必须对照各次调用的主体、期间和单位，不能把不同公司或不同口径拼成完整覆盖。余额同比需要相同月日的比较余额，半年报流量比较列不能补造上年同期资产负债。verify_financial_inputs失败时检查recovery候选，按其实际页码补读上下文、确认行列及期间后重新提交核对；不解除旧页symbolReview/OCR限制，不把候选或成功返回当成已独立核实。审计时核对financialCoverage和历史失败，逐项在缺口中说明仍缺期间、字段及对判断的影响；没有执行计算时不得声称已完成程序核算。';
