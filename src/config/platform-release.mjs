// Public feature version; saved research rules and model records keep their own versions.
export const platformVersion='5.2';
// The model configuration track evolves independently from the core release.
export const modelTrackVersion='M1.1';
